import { MapTaskTypePipe } from './map-task-type.pipe';

describe('MapTaskTypePipe', () => {
  it('create an instance', () => {
    const pipe = new MapTaskTypePipe();
    expect(pipe).toBeTruthy();
  });
});
