import { MapUserRolePipe } from './map-user-role.pipe';

describe('MapUserRolePipe', () => {
  it('create an instance', () => {
    const pipe = new MapUserRolePipe();
    expect(pipe).toBeTruthy();
  });
});
