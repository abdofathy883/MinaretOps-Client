import { MapLeadSourcePipe } from './map-lead-source.pipe';

describe('MapLeadSourcePipe', () => {
  it('create an instance', () => {
    const pipe = new MapLeadSourcePipe();
    expect(pipe).toBeTruthy();
  });
});
