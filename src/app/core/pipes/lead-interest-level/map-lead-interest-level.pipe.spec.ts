import { MapLeadInterestLevelPipe } from './map-lead-interest-level.pipe';

describe('MapLeadInterestLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new MapLeadInterestLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
