import { MapTaskPriorityPipe } from './map-task-priority.pipe';

describe('MapTaskPriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new MapTaskPriorityPipe();
    expect(pipe).toBeTruthy();
  });
});
