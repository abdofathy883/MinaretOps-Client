import { MapLeadNeedsPipe } from './map-lead-needs.pipe';

describe('MapLeadNeedsPipe', () => {
  it('create an instance', () => {
    const pipe = new MapLeadNeedsPipe();
    expect(pipe).toBeTruthy();
  });
});
