import { MapLeadFreelancePlatformPipe } from './map-lead-freelance-platform.pipe';

describe('MapLeadFreelancePlatformPipe', () => {
  it('create an instance', () => {
    const pipe = new MapLeadFreelancePlatformPipe();
    expect(pipe).toBeTruthy();
  });
});
