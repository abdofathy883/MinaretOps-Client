import { MapTaskStatusClassPipe } from './map-task-status-class.pipe';

describe('MapTaskStatusClassPipe', () => {
  it('create an instance', () => {
    const pipe = new MapTaskStatusClassPipe();
    expect(pipe).toBeTruthy();
  });
});
