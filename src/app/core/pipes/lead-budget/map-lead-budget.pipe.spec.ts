import { MapLeadBudgetPipe } from './map-lead-budget.pipe';

describe('MapLeadBudgetPipe', () => {
  it('create an instance', () => {
    const pipe = new MapLeadBudgetPipe();
    expect(pipe).toBeTruthy();
  });
});
