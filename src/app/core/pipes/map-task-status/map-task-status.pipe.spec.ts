import { MapTaskStatusPipe } from './map-task-status.pipe';

describe('MapTaskStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new MapTaskStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
