import { MapLeadContactStatusPipe } from './map-lead-contact-status.pipe';

describe('MapLeadContactStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new MapLeadContactStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
