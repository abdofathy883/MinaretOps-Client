import { MapLeadResponsibilityPipe } from './map-lead-responsibility.pipe';

describe('MapLeadResponsibilityPipe', () => {
  it('create an instance', () => {
    const pipe = new MapLeadResponsibilityPipe();
    expect(pipe).toBeTruthy();
  });
});
