import { MapLeadCurrentStatusPipe } from './map-lead-current-status.pipe';

describe('MapLeadCurrentStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new MapLeadCurrentStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
