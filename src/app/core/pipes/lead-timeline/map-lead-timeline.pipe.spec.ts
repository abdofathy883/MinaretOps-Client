import { MapLeadTimelinePipe } from './map-lead-timeline.pipe';

describe('MapLeadTimelinePipe', () => {
  it('create an instance', () => {
    const pipe = new MapLeadTimelinePipe();
    expect(pipe).toBeTruthy();
  });
});
