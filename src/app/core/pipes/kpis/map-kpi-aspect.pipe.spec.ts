import { MapKpiAspectPipe } from './map-kpi-aspect.pipe';

describe('MapKpiAspectPipe', () => {
  it('create an instance', () => {
    const pipe = new MapKpiAspectPipe();
    expect(pipe).toBeTruthy();
  });
});
